<div class="item">
  <span class="user">{{user}}</span>
  <span class="time">{{timestamp | format_time}}</span>
  <p class="text">{{content}}</p>
  <a href="#" class="reply">reply</a>

  {% for comment in childs %}
    {% assign item = comment %}
    {% include "snippet_comment" with item %}
  {% endfor %}
</div>